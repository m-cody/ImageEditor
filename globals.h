#ifndef GLOBALS_H
#define GLOBALS_H

const bool SHOW_DEBUG_CONSOLE = false;	//set this to true to display a console with your program that you can use standard c IO calls to
typedef unsigned char uchar;  //make it so we can use the shorthand "uchar" instead of unsigned char everywhere
const int COMPONENT_INCREASE_VALUE = 20;   //The amount that we increase a RGB component with one click of the button.
//const int NOISE_NUM = 350;
#endif
